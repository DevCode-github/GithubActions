name: "Workflow Variables"
on: 
  workflow_dispatch:
  # push:
env:
  cloud: google-cloud
jobs:
  greeting_job:
    runs-on: ubuntu-latest
    env: 
      greetings: greetings
    steps:
      - name: "Say Hello"
        run: echo $greeting $cloud ${{vars.PROJECT_NAME}}
  

      
